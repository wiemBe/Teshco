import 'package:flutter/material.dart';

class PersonPage2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: Text("Person 2"),
      centerTitle: true,
      backgroundColor: Colors.deepOrangeAccent,
    ),
  );
}