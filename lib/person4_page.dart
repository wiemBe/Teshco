import 'package:flutter/material.dart';

class PersonPage4 extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: Text("Person 4"),
      centerTitle: true,
      backgroundColor: Colors.deepOrangeAccent,
    ),
  );
}